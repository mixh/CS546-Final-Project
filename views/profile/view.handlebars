<nav class="navbar navbar-expand-md navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="/home/{{user._id}}">Meetup</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link" href="/home/{{user._id}}">Homepage</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/matches/{{user._id}}">Matches</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/potentialMatches/{{user._id}}?distance=all">Potential Matches</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/profile/{{user._id}}">Profile</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/messages/{{user._id}}/messages">Messages</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/logout">Logout</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div>
  <h1>Profile</h1>
  {{#if user.image.path}}
    <img src="/{{user.image.destination}}/{{user.image.filename}}" alt="Profile image">
  {{else}}
    <p>No image uploaded</p>
  {{/if}}
  <p>Name: {{user.name}}</p>
  <p>Age: {{user.age}}</p>
  <p>City: {{user.city_name}}</p>
  <p>Zip : {{user.zip}}</p>
  <p>Bio: {{user.bio}}</p>
  <p>University : {{user.university}} </p>
  <p>Bucketlist : {{user.bucketlist}}</p>
  <p>Gym : {{user.gym}} </p>
  <p>Work : {{user.work}} </p>
</div>
  {{#if user.isPaused}}
  <form method="POST" action="/profile/{{user._id}}/pause">
    <button type="submit">Unpause profile</button>
  </form><br><br>
  {{else}}
  <form method="POST" action="/profile/{{user._id}}/pause">
    <button type="submit">Pause profile</button>
  </form><br><br>
  {{/if}}
  <div>
  <a href="/profile/{{user._id}}/edit?gender={{user.gender}}&university={{user.university}}&bucketlist={{user.bucketlist}}&gym={{user.gym}}" class="btn btn-primary">Edit profile</a>
  </div>